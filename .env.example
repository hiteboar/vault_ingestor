# Token del bot (creado con @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABCDEF_your_token_here

# Carpeta donde guardar los archivos (se creará si no existe)
STORAGE_DIR=./vault_storage

# Log de metadatos (JSON Lines)
META_LOG=./vault_storage/metadata.jsonl

# Tamaño máximo por archivo en bytes (0 = sin límite)
MAX_BYTES=0

# Opcional: permitir solo estos chat IDs (separados por coma). Vacío = permite cualquiera.
# Para saber tu chat id: arranca el bot y envíale un mensaje; se imprimirá en consola.
ALLOWED_CHAT_IDS=

# Si es false, no aceptamos "photo" (porque puede venir comprimida). Solo "document" y "video".
ALLOW_COMPRESSED_PHOTOS=true

# Carpeta base "por defecto" (dentro de STORAGE_DIR)
DEFAULT_CONTEXT=default

# API Key de Google Gemini para el Agente IA
GEMINI_API_KEY=your_gemini_api_key_here
# Modelo de IA a utilizar (ej: gemini-2.0-flash-exp, gemini-1.5-flash)
AI_MODEL=gemini-3.1-flash-lite-preview
